//

#include <stdio.h>

int main(void) {

    int input;
    int success = scanf("%d", &input);
    
    while (success == 1) {
        printf("You entered %d!\n", input);
        success = scanf("%d", &input);
    }
    
    return 0;
}
